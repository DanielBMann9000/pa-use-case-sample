<Page
    x:Class="C_Sharp_Windows_Store_Sample.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    
    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Border Padding="10">
            <StackPanel Grid.Row="0">
                <TextBlock Text="PreEmptive Solutions" Style="{StaticResource SubheaderTextStyle}" Padding="5"/>
                <TextBlock Text="C# Windows Store Sample" Style="{StaticResource HeaderTextStyle}" Padding="10"/>
            </StackPanel>
        </Border>
        <StackPanel Grid.Row="1">
            <Button Content="Run Sample" FontSize="50" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="RunSample_OnClick"></Button>
            <Button Content="Throw Unhandled Exception" FontSize="50" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="ThrowException_OnClick"></Button>
        </StackPanel>
    </Grid>
</Page>
